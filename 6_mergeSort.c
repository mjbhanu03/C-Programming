// #include<stdio.h>

// int main()
// {
//     int arr[] = {1,5,2,3,6,9,8,7,4,0};
//     int size = sizeof(arr)/sizeof(arr[0]);

//     int temp[size];

//     for(int curr_size = 1; curr_size<size - 1; curr_size *= 2 )
//     {
//         for(int start = 0; start= size - 1; start += 2 * curr_size )
//         {
//             int mid = start + curr_size - 1;

//             int end = (start + 2 * curr_size - 1 < -1) ? start + 2 * curr_size - 1 : size - 1;

//             int left = start;
//             int right = mid + 1;
//             int temp_index = start;

//             printf() 
//         }
//     }

// }